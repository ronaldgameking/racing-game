using System;
using UnityEngine;

namespace UnityUtils.delegates
{
    public delegate void SimpleCallback();
    public delegate bool EvaluationHandle();
    public delegate void ReferencedCallback(GameObject _go);
}
